<Window x:Class="TestApp.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Online PostgreSQL Test Application" Height="580" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="PostgreSQL Client-Server Test" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" Foreground="#1E293B"/>

        <!-- Form Fields -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Name:" FontWeight="SemiBold" Margin="0,0,0,4" Foreground="#334155"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" Margin="0,0,0,12" FontSize="14"/>

            <TextBlock Text="Comment:" FontWeight="SemiBold" Margin="0,0,0,4" Foreground="#334155"/>
            <TextBox Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" Margin="0,0,0,12" FontSize="14" AcceptsReturn="True" TextWrapping="Wrap" Height="60"/>

            <StackPanel Orientation="Horizontal">
                <Button Content="Submit" Command="{Binding SubmitCommand}" Padding="20,8" Margin="0,0,10,0" FontWeight="Bold" Background="#2563EB" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="15,8" Background="#64748B" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </StackPanel>

        <!-- Divider -->
        <Separator Grid.Row="2" Margin="0,0,0,15"/>

        <!-- Table Header -->
        <TextBlock Grid.Row="3" Text="Submitted Comments" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="#1E293B"/>

        <!-- DataGrid -->
        <DataGrid Grid.Row="4" ItemsSource="{Binding Comments}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="White" RowHeight="32" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" Width="*"/>
                <DataGridTextColumn Header="Created At" Binding="{Binding FormattedCreatedAt}" Width="180"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="5" Margin="0,10,0,0" Background="#F1F5F9">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#475569" FontSize="12"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
